// eslint-disable-next-line no-shadow
const enum QueueEvent {
    SongEnd = "songEnd",
    SongStart = "songStart",
    QueueEnd = "queueEnd",
    SongError = "songError",
}

export default QueueEvent;
